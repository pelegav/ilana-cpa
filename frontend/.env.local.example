# Copy this file to .env.local and fill in real values.

# The Spring Boot backend's base URL (server-only — the browser never calls it
# directly; Next.js Server Actions/Route Handlers proxy to it).
BACKEND_API_URL=http://localhost:8082
